package test.dummy;

//used to demonstrate that Mockito can easily create fake implementations (i.e., Mocks)
//from a given interface (also works for abstracted classes)
public interface IDummyClassForTests {

	int div(int a, int b);

}